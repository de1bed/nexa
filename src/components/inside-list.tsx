"use client";import { useDemo } from "./demo-provider";import { Clock3,LogOut,Users } from "lucide-react";
export function InsideList(){const {state,updateVisit}=useDemo();const inside=state.visits.filter(v=>v.status==="checked_in");return <><header className="mb-7"><p className="text-sm font-medium text-[#10cfc9]">Estado en tiempo real</p><h1 className="mt-2 text-3xl font-semibold">Personas dentro <span className="text-[#10cfc9]">{inside.length}</span></h1></header>{inside.length===0?<div className="rounded-2xl bg-white/5 py-14 text-center"><Users className="mx-auto text-slate-500" size={38}/><p className="mt-4 font-medium">Las instalaciones están vacías</p></div>:<div className="space-y-3">{inside.map(v=><div key={v.id} className="rounded-2xl bg-white p-5 text-[#071426]"><div className="flex items-center gap-3"><span className="grid size-12 place-items-center rounded-full bg-slate-100 font-semibold">{v.visitorName.split(" ").map(x=>x[0]).slice(0,2)}</span><div className="min-w-0 flex-1"><p className="truncate font-semibold">{v.visitorName}</p><p className="truncate text-sm text-slate-500">{v.company} · {v.hostName}</p></div></div><div className="mt-4 flex items-center justify-between border-t border-slate-100 pt-4"><span className="text-xs text-slate-500"><Clock3 className="mr-1 inline" size={14}/>Entrada {new Intl.DateTimeFormat("es-MX",{timeStyle:"short"}).format(new Date(v.checkedInAt!))}</span><button onClick={()=>updateVisit(v.id,{status:"checked_out",checkedOutAt:new Date().toISOString()},{type:"check_out",actor:"Carlos Mendoza"})} className="inline-flex h-10 items-center gap-2 rounded-xl bg-[#071426] px-4 text-sm font-semibold text-white"><LogOut size={16}/>Salida</button></div></div>)}</div>}</>}
