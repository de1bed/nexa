"use client";import { useState } from "react";import { useRouter } from "next/navigation";import Link from "next/link";import { Brand } from "./brand";import { ArrowRight,Eye,EyeOff,LoaderCircle,LockKeyhole } from "lucide-react";import { createClient,hasSupabaseConfig } from "@/lib/supabase/client";
export function LoginForm(){const router=useRouter();const [email,setEmail]=useState("admin@novalogistics.demo");const [password,setPassword]=useState("NexaDemo2026!");const [show,setShow]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{if(hasSupabaseConfig()&&process.env.NEXT_PUBLIC_DEMO_MODE!=="true"){const {error}=await createClient().auth.signInWithPassword({email,password});if(error)throw error}await new Promise(r=>setTimeout(r,300));router.push(email.startsWith("guardia")?"/guard/scan":"/app/dashboard")}catch{setError("No pudimos iniciar sesión. Revisa tus datos e intenta de nuevo.")}finally{setBusy(false)}}return <main className="grid min-h-screen bg-white lg:grid-cols-2"><section className="flex items-center justify-center px-6 py-12"><div className="w-full max-w-md"><Brand/><div className="mt-14"><p className="text-sm font-medium text-[#0eaaa5]">Bienvenido de nuevo</p><h1 className="mt-2 text-4xl font-semibold tracking-[-.04em]">Accede a tu espacio</h1><p className="mt-3 text-slate-500">Gestiona visitas y accesos de tu organización.</p></div><form onSubmit={submit} className="mt-8 space-y-5"><label className="block"><span className="mb-2 block text-sm font-medium">Correo</span><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required className="h-12 w-full rounded-xl border border-slate-200 px-4 outline-none focus:border-[#10aaa5]"/></label><label className="block"><span className="mb-2 flex justify-between text-sm font-medium">Contraseña <Link href="/forgot-password" className="font-normal text-blue-600">¿La olvidaste?</Link></span><span className="relative block"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?"text":"password"} required className="h-12 w-full rounded-xl border border-slate-200 px-4 pr-12 outline-none focus:border-[#10aaa5]"/><button type="button" aria-label="Mostrar contraseña" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-slate-400">{show?<EyeOff/>:<Eye/>}</button></span></label>{error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={busy} className="flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-[#071426] font-semibold text-white disabled:opacity-60">{busy?<LoaderCircle className="animate-spin"/>:<>Iniciar sesión <ArrowRight size={18}/></>}</button></form><div className="mt-8 rounded-2xl border border-cyan-200 bg-cyan-50 p-4 text-sm"><p className="font-semibold">Credenciales demostrativas</p><p className="mt-2 text-cyan-900">Admin: admin@novalogistics.demo</p><p className="text-cyan-900">Guardia: guardia1@novalogistics.demo</p><p className="mt-1 text-xs text-cyan-700">Contraseña: NexaDemo2026!</p></div></div></section><section className="relative hidden overflow-hidden bg-[#071426] p-12 text-white lg:flex lg:flex-col lg:justify-end"><div className="absolute -right-40 -top-40 size-[500px] rounded-full bg-[#10cfc9]/20 blur-3xl"/><div className="relative max-w-lg"><span className="grid size-14 place-items-center rounded-2xl bg-[#10cfc9] text-[#071426]"><LockKeyhole/></span><blockquote className="mt-8 text-4xl font-medium leading-tight tracking-[-.03em]">“Una recepción más ágil empieza antes de que llegue el visitante.”</blockquote><p className="mt-6 text-slate-400">NEXA VISIT · Control inteligente de accesos</p></div></section></main>}
