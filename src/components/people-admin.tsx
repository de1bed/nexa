"use client";import Link from "next/link";import { useDemo } from "./demo-provider";import { Clock3,LogOut,Users } from "lucide-react";
export function PeopleAdmin(){const {state,updateVisit}=useDemo();const rows=state.visits.filter(v=>v.status==="checked_in");return <><header className="mb-8"><p className="mb-2 text-sm font-medium text-[#0eaaa5]">Tiempo real</p><h1 className="text-3xl font-semibold">Personas actualmente dentro</h1><p className="mt-2 text-slate-500">{rows.length} visitantes en las instalaciones.</p></header>{rows.length===0?<div className="rounded-2xl border bg-white py-16 text-center"><Users className="mx-auto text-slate-300" size={44}/><p className="mt-4 font-medium">No hay visitantes dentro</p></div>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{rows.map(v=><article key={v.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><span className="grid size-12 place-items-center rounded-full bg-cyan-50 font-semibold text-cyan-700">{v.visitorName.split(" ").map(x=>x[0]).slice(0,2)}</span><div><Link href={`/app/visits/${v.id}` as never} className="font-semibold hover:text-blue-600">{v.visitorName}</Link><p className="text-sm text-slate-500">{v.company}</p></div></div><div className="mt-5 rounded-xl bg-slate-50 p-4 text-sm"><p>Anfitrión: <b>{v.hostName}</b></p><p className="mt-2 text-slate-500"><Clock3 className="mr-1 inline" size={15}/>Entrada {new Intl.DateTimeFormat("es-MX",{timeStyle:"short"}).format(new Date(v.checkedInAt!))}</p></div><button onClick={()=>updateVisit(v.id,{status:"checked_out",checkedOutAt:new Date().toISOString()},{type:"check_out",actor:"Elena Torres"})} className="mt-4 inline-flex h-10 w-full items-center justify-center gap-2 rounded-xl border border-slate-200 text-sm font-semibold"><LogOut size={16}/>Registrar salida</button></article>)}</div>}</>}
