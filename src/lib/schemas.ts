import { z } from "zod";
export const invitationSchema=z.object({visitorName:z.string().trim().min(2,"Ingresa el nombre"),email:z.email("Correo inválido"),phone:z.string().optional(),company:z.string().min(2),location:z.string().min(1),date:z.string().min(1),startTime:z.string().min(1),endTime:z.string().min(1),purpose:z.string().min(2),notes:z.string().max(500).optional(),accessRequirements:z.string().max(500).optional(),sendEmail:z.boolean()}).refine(v=>v.endTime>v.startTime,{message:"La hora final debe ser posterior",path:["endTime"]});
export const visitorRegistrationSchema=z.object({fullName:z.string().min(2),email:z.email(),phone:z.string().min(7),company:z.string().min(2),documentType:z.string().min(1),documentNumber:z.string().optional(),vehiclePlate:z.string().optional(),visitorNotes:z.string().max(500).optional(),consent:z.literal(true,{error:"Debes aceptar el aviso de privacidad"})});
export const manualVisitSchema=z.object({visitorName:z.string().min(2),email:z.email().or(z.literal("")),company:z.string().min(2),hostName:z.string().min(1),purpose:z.string().min(2),consent:z.boolean().refine(Boolean,"Confirma el consentimiento")});
